#
name: Test Docker Images

# Configures this workflow to run every time a change is pushed to the branch called `release`.
on:
  pull_request:
    branches: [ main ]

# Defines two custom environment variables for the workflow. These are used for the Container registry domain, and a name for the Docker image that this workflow builds.
env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

# There is a single job in this workflow. It's configured to run on the latest available version of Ubuntu.
jobs:
  build-and-test-image:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - variant: cpu
            enable_cuda: "0"
            base_image: ubuntu:20.04
          - variant: mps
            enable_cuda: "0"
            base_image: ubuntu:20.04
          - variant: cuda
            enable_cuda: "1"
            base_image: nvidia/cuda:12.0.0-runtime-ubuntu20.04
    # Sets the permissions granted to the `GITHUB_TOKEN` for the actions in this job.
    permissions:
      contents: read
      packages: read
      # 
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      # This step uses the `docker/build-push-action` action to build the image, based on your repository's `Dockerfile`. If the build succeeds, it pushes the image to GitHub Packages.
      # It uses the `context` parameter to define the build's context as the set of files located in the specified path. For more information, see "[Usage](https://github.com/docker/build-push-action#usage)" in the README of the `docker/build-push-action` repository.
      # It builds CPU, MPS, and CUDA variants to validate Docker build paths.
      - name: Build Docker image (${{ matrix.variant }})
        uses: docker/build-push-action@f2a1d5e99d037542a71f64918e516c093c6f3fc4
        with:
          context: .
          push: false
          build-args: |
            ENABLE_CUDA=${{ matrix.enable_cuda }}
            BASE_IMAGE=${{ matrix.base_image }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:pr-${{ github.event.pull_request.number }}-${{ matrix.variant }}
