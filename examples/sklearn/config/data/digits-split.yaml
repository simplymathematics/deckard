# Use with: sample=split
# e.g. python -m deckard data=digits-split sample=split
dataset_name: digits
test_size: 0.2
val_size: 0.1
random_state: 42
classifier: true
stratify: true
alias: digits-split
