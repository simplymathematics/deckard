name: adult
sampler:
  name: deckard.data.sample.SplitSampler
  test_size: 0.2
  random_state: 42
  stratify: false
classifier: true
alias: ${.name}
pipeline:
  imputer:
    name: sklearn.impute.SimpleImputer
    strategy: mean
