data: ${data}
library : sklearn
init:
    name : gzip_classifier.GzipKNN
    k : 1
    m: -1
    metric : gzip
    symmetric : false
    distance_matrix :  ${files.directory}/${files.model_dir}/${model.init.metric}/${data.sample.train_size}-${data.sample.test_size}/${data.sample.random_state}.npz
    similarity : True
    min_max_scale  : True
    condensing_method : None