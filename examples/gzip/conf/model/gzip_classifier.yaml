data: ${data}
library : sklearn
init:
    name : gzip_classifier.GzipClassifier
    k : 1
    m: -1
    method : random
    distance_matrix : ${files.directory}/${files.model_dir}/${dataset}/${model_name}/${model.init.compressor}/${data.sample.train_size}--${data.sample.test_size}-${data.sample.random_state}.npz
    compressor : gzip
art:
    _target_ : deckard.base.model.art_pipeline.ArtPipeline
    library : sklearn
    initialize:
      nb_classes: 3
