data: ${data}
library : sklearn
init:
    name : gzip_classifier.GzipLogisticRegressor
    metric : gzip
    max_iter : 1000
    symmetric : false
    modified : false
    distance_matrix_train :  ${files.directory}/${files.model_dir}/${model.init.metric}/${model.init.modified}/train-${data.sample.train_size}-${data.sample.test_size}-${data.sample.random_state}.npz
    distance_matrix_test : ${files.directory}/${files.model_dir}/${model.init.metric}/${model.init.modified}/test-${data.sample.train_size}-${data.sample.test_size}-${data.sample.random_state}.npz
    transform : None
