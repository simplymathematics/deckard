_target_: deckard.base.data.Data
name: raw_data/ddos.csv
target: 'Label'
drop:
  - 'Timestamp' # Drop the timestamp column
sample:
    random_state : 0
    train_size : 100
    test_size : 100
    stratify: True
    shuffle : True