_target_: deckard.file.FileConfig
data_file: output/${dataset}/data/${dataset}-${data.sampler.random_state}-${data.sampler.train_size}-${data.sampler.test_size}-${oc.select:data.split,0}.pkl
score_file: output/${dataset}/reports/search/${model_name}/${algorithm}/${data.sampler.train_size}/${oc.select:data.split,0}/${data.sampler.test_size}/${data.sampler.random_state}/${model.init.metric}/attack/score_dict.json
params_file: output/${dataset}/params.yaml
test_predictions_file: output/${dataset}/reports/search/${model_name}/${algorithm}/${data.sampler.train_size}/${oc.select:data.split,0}/${data.sampler.test_size}/${data.sampler.random_state}/${model.init.metric}/attack/predictions.json
log_file: output/${dataset}/reports/search/${model_name}/${algorithm}/${data.sampler.train_size}/${oc.select:data.split,0}/${data.sampler.test_size}/${data.sampler.random_state}/${model.init.metric}/logs/run.log
error_file: output/${dataset}/reports/search/${model_name}/${algorithm}/${data.sampler.train_size}/${oc.select:data.split,0}/${data.sampler.test_size}/${data.sampler.random_state}/${model.init.metric}/logs/error.log
